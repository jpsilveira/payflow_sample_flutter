import 'package:flutter/material.dart';

class BillTileWidget extends StatelessWidget {
  const BillTileWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
    // stopped here 012158
  }
}
